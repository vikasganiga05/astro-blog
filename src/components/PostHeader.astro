---
import { Image } from "@astrojs/image/components";

import { slugify, formatDate } from "../scripts/utils";

const { title, image, date, category, author } = Astro.props;
---

<header>
  <div class="container">
    <small>
      <a href={`/category/${slugify(category)}/`} class="badge">{category}</a>
    </small>
    <h1 class="h2">{title}</h1>
    <p>
      by <a href={`/author/${slugify(author)}/`}>{author}</a> • {
        formatDate(date)
      }
    </p>
  </div>
  <!-- Astro image integration is not working for some reason. -->
  <!-- <Image
      src={image.src}
      alt={image.alt}
      width={1200}
      height={600}
      format="avif"
      fit="cover"
      quality={80}
      aspectRatio="5:2"
      class="hero-image"
    /> -->
  <img
    src={image.src}
    alt={image.alt}
    width={1200}
    height={600}
    class="hero-image"
  />
</header>
